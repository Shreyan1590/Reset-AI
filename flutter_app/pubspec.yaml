name: reset_ai
description: RESET AI – Context Loss Detection & Cognitive Recovery System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase packages - latest compatible with Flutter 3.35
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  # Keep google_sign_in at 6.x to avoid breaking API changes
  google_sign_in: ^6.3.0
  provider: ^6.1.1
  go_router: ^13.0.0
  http: ^1.1.2
  shared_preferences: ^2.2.2
  # fl_chart at 1.x
  fl_chart: ^1.1.1
  intl: ^0.19.0
  lottie: ^3.0.0
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  uuid: ^4.2.1
  google_generative_ai: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/
